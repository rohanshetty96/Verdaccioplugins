import {Config} from '@verdaccio/types';

export declare const REGISTRY_DOMAIN = "https://registry.npmjs.org";
export default function loadPlugin<T>>(config: Config, pluginConfigs: any, params : any, sanity {
  foo: string;
}
